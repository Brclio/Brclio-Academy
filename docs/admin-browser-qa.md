# 管理后台浏览器验收

核验时间：2026-09-11 00:01–00:14（Asia/Shanghai）。环境：本地开发服务 `http://localhost:3000`、SQLite；使用 Playwright CLI 独立会话 `academy-admin-qa`。本报告涉及真实界面操作与持久化结果，未进行远端部署或写入。

## 已执行结果

| 项目 | 实际操作与证据 | 结果 |
| --- | --- | --- |
| 管理员登录 | 使用已有本地测试账号登录，进入「内容与成员管理」 | 通过 |
| 创建课程 | 填写课程名称、介绍、分类、学习时间、学习目标，选择 Web 封面主题；创建章节和图文课时，勾选发布并保存 | 通过；公开目录显示新课程及对应封面主题 |
| 课时排序 | 新增第二个图文课时，将第二课上移后保存 | 通过；目录显示「第二课 → 第一课」，两课 ID 保持不变 |
| 课堂内容 | 从公开课程详情进入排到第一位的第二课 | 通过；课堂标题、序号与 Markdown 正文正确对应 |
| YouTube 链接输入 | 将样例课程第一个视频填写为完整 `https://www.youtube.com/watch?v=u13DKz6cnbo` 后保存 | 通过；存储值规范化为 `u13DKz6cnbo`；逐字段比较确认样例课程除 `updatedAt` 外完全一致 |
| 独立公开文章 | 在后台创建正文并发布 | 通过；匿名读取文章接口 HTTP 200，返回保存的 Markdown 正文 |
| 草稿专栏隔离 | 创建未发布专栏，将已发布文章关联进去 | 通过；公开目录不再包含文章，匿名正文请求 HTTP 404 |
| 专栏发布 | 在后台勾选发布专栏并保存 | 通过；文章恢复进入公开目录，匿名正文请求 HTTP 200 |
| VIP 文章 | 将文章阅读权限改为 VIP 并保存 | 通过；目录保留标题摘要，匿名正文请求 HTTP 401；有效 VIP 在浏览器看到真实正文 |
| 文章下架 | 取消发布文章并保存 | 通过；公开目录隐藏文章，匿名正文请求 HTTP 404 |
| 操作记录 | 打开「操作记录」 | 通过；真实课程、文章、专栏保存操作显示为中文记录并关联正确对象 |
| 响应式 | 检查 1440×1080 桌面、390×844 移动视口及完整移动页面截图 | 通过；移动端 `document.scrollWidth = innerWidth = 390`，无页面横向溢出 |

本轮未修改共享测试账号的密码、VIP 有效期或权限；会员开通/撤销不计入本轮浏览器验收。YouTube 此项只核验后台 URL 输入、规范化和保存，不代表视频播放已验证。全量视频元数据检查见 [VIDEO-CHECK.md](VIDEO-CHECK.md)。

## 清理与保留

测试过程中创建的以下记录已按确切 ID、标题核对后，在 SQLite 事务中清理：

- 课程：`course-94edac91-40f9-44e4-954c-f07de9e7c3a5`
- 文章：`article-d4efa201-630f-47bd-8349-1e90d799c414`
- 专栏：`column-d05aa3d4-f99d-4347-8fff-05ca96d4bd74`

清理范围包括上述临时课程的关联进度、笔记、收藏和指向这三个对象的 8 条测试审计。所有用户和其他记录保持原样。样例课程的视频规范化保存记录保留。

清理后通过公开 API 再次确认：**4 门课程、6 篇文章、2 个专栏，临时 QA 内容为 0**。本轮没有发现需要修改源码的新缺陷。

## 截图

- [桌面管理后台](previews/admin-desktop.png)：1440×1080。
- [移动管理后台](previews/admin-mobile.png)：390 像素宽完整页面，已核对成员信息与 VIP 设置控件布局。

主要浏览器快照保存在本地忽略目录 `.playwright-cli/`：课程创建 `page-2026-09-10T16-05-17-604Z.yml`、课程排序后详情 `page-2026-09-10T16-06-26-435Z.yml`、YouTube URL 保存 `page-2026-09-10T16-08-06-139Z.yml`、文章下架 `page-2026-09-10T16-12-32-725Z.yml`、操作记录 `page-2026-09-10T16-12-36-008Z.yml`。
